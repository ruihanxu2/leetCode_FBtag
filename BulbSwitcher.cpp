class Solution {
public:
	int bulbSwitch(int n) {
		// No p bulb will be toggled at the rounds that is its factor
		// If its factor is odd, it will be on
		// only perfect squares have odd factor
		return sqrt(n);
	}
};